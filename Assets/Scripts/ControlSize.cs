using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;
public class ControlSize : MonoBehaviour
{
   public Oculus.Interaction.Grabbable Grabbable; 
    // Update is called once per frame
    void Update()
    {
        /*
        if (OVRInput.Get(OVRInput.Button.One))
        {
            gameObject.transform.localScale += Vector3.one * Time.deltaTime;
        }
        if (OVRInput.Get(OVRInput.Button.Two))
        {
            gameObject.transform.localScale -= Vector3.one* Time.deltaTime;
        }
        
        if (OVRInput.Get(OVRInput.Button.Three))
        {
            gameObject.transform.Translate(Vector3.forward  * Time.deltaTime, Space.Self);
        }
        if (OVRInput.Get(OVRInput.Button.Four))
        {
            gameObject.transform.Translate(-Vector3.forward  * Time.deltaTime, Space.Self);
        }
        */
    }
}
